<%= form_for ([@picture, @comment]) do |f| %>

  <div>
    <%= f.label "Enter your comment"%><br>
    <%= f.text_area "content"%>
  </div>

  <div class="actions">
      <%= f.submit %>
  </div>

  <% end %>
